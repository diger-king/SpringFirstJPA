spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa1?serverTimezone=Asia/Seoul
    username: root
    password: 112233
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug